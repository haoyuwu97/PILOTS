This directory is the HTML output directory for the Sphinx manual.

To build locally:
  ./docs/build_docs.sh

For an online manual, enable GitHub Pages and use the workflow:
  .github/workflows/docs-pages.yml

The published URL will be:
  https://<owner>.github.io/<repo>/
